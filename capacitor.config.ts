import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sportivemem-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
